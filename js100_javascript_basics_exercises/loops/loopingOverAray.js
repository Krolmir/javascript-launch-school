let array = [1, 2, 3, 4];
let index = 0;

// Write a while loop that logs the elements of array at each index, and termin-
// ates after logging the last element of the array

while (index < array.length) {
  console.log(array[index]);
  index++;
}